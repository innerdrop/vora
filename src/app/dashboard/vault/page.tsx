import PlaceholderPage from "@/components/PlaceholderPage";

export const metadata = {
    title: "Vault Médico | VORA",
};

export default function VaultPage() {
    return (
        <PlaceholderPage
            title="Vault Médico"
            description="Tu espacio seguro para almacenar y compartir documentación médica de forma encriptada."
        />
    );
}
